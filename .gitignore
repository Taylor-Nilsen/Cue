node_modules/
dist/
.DS_Store
*.log

# Vendored at install time by scripts/copy-assets.mjs — ~21 MB of wasm and
# OCR language data that has no business in the repo.
public/tess/

# Scripts kept around for testing. Real scripts are under copyright and some
# run to 12 MB of scan — they don't belong in the repo.
test-scripts/
public/__test-*
